# ControlDESymulation - Development Dependencies
# Install with: pip install -r requirements-dev.txt
# Or use: pip install -e ".[dev]"

# ============================================================================
# TESTING
# ============================================================================
pytest>=8.0
pytest-cov>=4.1          # Coverage reporting
pytest-xdist>=3.5        # Parallel test execution
pytest-timeout>=2.2      # Test timeouts
pytest-benchmark>=4.0    # Performance benchmarking
pytest-mock>=3.12        # Mocking utilities

# ============================================================================
# CODE QUALITY & LINTING
# ============================================================================
black>=24.0              # Code formatter
isort>=5.13              # Import sorting
ruff>=0.3                # Fast Python linter (replaces flake8, pylint partially)
mypy>=1.8                # Static type checker

# Alternative/additional linters (optional, ruff covers most of this):
flake8>=7.0
pylint>=3.0

# ============================================================================
# SECURITY
# ============================================================================
bandit>=1.7              # Security issue scanner
safety>=3.0              # Dependency vulnerability scanner

# ============================================================================
# PRE-COMMIT HOOKS
# ============================================================================
pre-commit>=3.6          # Git pre-commit hook framework

# ============================================================================
# DOCUMENTATION
# ============================================================================
sphinx>=7.2              # Documentation generator
sphinx-rtd-theme>=2.0    # Read the Docs theme
sphinx-autodoc-typehints>=2.0  # Type hint support in docs
myst-parser>=2.0         # Markdown support in Sphinx

# ============================================================================
# TYPE STUBS (for better type checking)
# ============================================================================
types-requests>=2.31
types-setuptools>=69.0

# ============================================================================
# BUILD & PACKAGING
# ============================================================================
build>=1.0               # PEP 517 build frontend
twine>=5.0               # Package upload to PyPI
wheel>=0.42              # Wheel package format

# ============================================================================
# JUPYTER & NOTEBOOKS (for examples and tutorials)
# ============================================================================
jupyter>=1.0
jupyterlab>=4.1
ipykernel>=6.29
ipywidgets>=8.1
notebook>=7.1

# ============================================================================
# OPTIONAL INTEGRATORS (for testing)
# ============================================================================
torchdiffeq>=0.2.3       # PyTorch ODE solver
torchsde>=0.2.6          # PyTorch SDE solver
diffrax>=0.6.0           # JAX ODE/SDE solver (requires JAX)
diffeqpy>=2.0          # Julia wrapper (requires Julia installation)

# ============================================================================
# OPTIONAL OPTIMIZATION LIBRARIES (for testing MPC, etc.)
# ============================================================================
cvxpy>=1.4               # Convex optimization
casadi>=3.6            # Nonlinear optimization (large dependency)

# ============================================================================
# VISUALIZATION (for testing plotting utilities)
# ============================================================================
plotly>=5.18
matplotlib>=3.8

# ============================================================================
# JAX (for testing JAX backend)
# ============================================================================
jax>=0.4.30
jaxlib>=0.4.30

# ============================================================================
# CLASSICAL CONTROL (for testing control utilities)
# ============================================================================
control>=0.10

# ============================================================================
# UTILITIES
# ============================================================================
tqdm>=4.66               # Progress bars
rich>=13.7               # Rich terminal output
click>=8.1               # CLI framework (if adding CLI tools)

# ============================================================================
# INSTALLATION INSTRUCTIONS
# ============================================================================
#
# Install all development dependencies:
#   pip install -r requirements-dev.txt
#
# Or use the pyproject.toml optional dependencies:
#   pip install -e ".[dev]"
#
# For complete development setup with all optional features:
#   pip install -e ".[all]"
#