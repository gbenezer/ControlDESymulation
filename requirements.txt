# ControlDESymulation - Runtime Dependencies
# Install with: pip install -r requirements.txt
# torch is currently required but will be converted to
# an optional library after refactoring technical debt
# and added to requirements-dev.txt

# ============================================================================
# CORE DEPENDENCIES (Required)
# ============================================================================

# Numerical computing
numpy>=2.0,<3.0

# Symbolic mathematics
sympy>=1.12

# Scientific computing
scipy>=1.10

# PyTorch backend (deep learning, GPU support)
torch>=2.0

# ============================================================================
# OPTIONAL DEPENDENCIES
# ============================================================================
# These are optional but recommended for full functionality.
# Install specific groups with: pip install -e ".[jax,viz,control]"
# Or install everything with: pip install -e ".[all]"

# JAX backend (functional programming, JIT compilation, TPU support)
# Uncomment to install:
# jax>=0.4.30
# jaxlib>=0.4.30

# Visualization
# Uncomment to install:
# plotly>=5.0
# matplotlib>=3.5

# Classical control theory utilities
# Uncomment to install:
# control>=0.10

# ODE/SDE integration backends
# Uncomment to install:
# torchdiffeq>=0.2.3  # PyTorch ODE solver
# torchsde>=0.2.6     # PyTorch SDE solver
# diffrax>=0.6.0      # JAX ODE/SDE solver
# diffeqpy>=2.0       # Julia DifferentialEquations.jl wrapper

# Optimization libraries (for MPC, trajectory optimization)
# Uncomment to install:
# cvxpy>=1.4          # Convex optimization
# casadi>=3.6         # Nonlinear optimization

# ============================================================================
# INSTALLATION INSTRUCTIONS
# ============================================================================
#
# Standard installation (core only):
#   pip install -r requirements.txt
#
# Development installation with all optional dependencies:
#   pip install -e ".[all]"
#
# Custom installation (pick what you need):
#   pip install -e ".[jax,viz]"
#
# Available optional groups: jax, viz, control, integrators, optimization, dev, all
#